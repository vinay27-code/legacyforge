export const environment = {
  production: true,
  // Set at build time in Vercel via NG_APP_API_BASE_URL, or hard-code your Cloud Run URL here.
  apiBaseUrl: 'https://legacyforge-backend-xxxxx.a.run.app'
};
